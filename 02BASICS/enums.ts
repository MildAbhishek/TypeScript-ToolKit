enum RoomNo {
    AISLE = 1001,
    MIDDLE, // 1002
    WINDOW, // 1003
}

enum SeatChoice {
    AISLE = 'aisle',
    MIDDLE = 'middle',
    WINDOW = 'window',
}

enum SeatChoices {
    AISLE = 'aisle',
    MIDDLE = 1002,
    WINDOW, // 1003
}